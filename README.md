# Build-and-Query-a-Feature-Store-for-ML-Training-Serving
Create a reusable feature store to serve features for both training and inference using a modern tool like Feast.  **Requirements:** - Define entities and feature sets (e.g., customer, transaction) - Use Feast for feature store setup - Materialize features from batch + stream sources - Query feature vectors for online/offline use
